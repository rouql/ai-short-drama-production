---
name: ai-short-drama-production
description: 完成或修复 AI 短剧的故事架构、正常短剧剧本、导演分解、全剧资产生图提示词和分集视频提示词。采用分阶段专业创作与轻量质量门禁；只在用户要求的阶段输出文本，不直接生成图片或视频。
---

# AI 短剧生产

## 目标

这不是格式模板，而是一套量产型创作 Skill。它必须同时保证：故事有发动机、单集有钩子和情绪变化、剧本自然可读、导演拆解可执行、资产能够稳定复用、提示词能够直接投喂模型。

专业知识按阶段加载，不能为了省 Token 删除创作方法，也不能为了审查制造重复材料。

## 运行模式

Skill只有两种模式：

- **生产模式（默认）**：只创作、检查和返工用户项目产物。全局Skill入口、阶段参考、模板和校验脚本全部只读；用户说“检查、返工、继续、进入下一阶段”只授权处理项目产物，绝不授权修改Skill。
- **维护模式**：只有用户明确要求检查、升级、修改或封版Skill时进入。此模式只处理Skill，不修改任何剧本项目；先确定完整范围并获得批准，再备份、一次修改、完整验证和打包，完成后退出维护模式。

项目生产中发现疑似规则缺陷时，记录最短复现并向用户报备；能在不降质的前提下继续生产就继续，现行规则会强迫降质则停止受影响部分。不得把正式项目当成Skill试验场，也不得边返工产物边暗改全局规则。

## 路由

每次任务固定读取 [references/quality-gate.md](references/quality-gate.md)，再读取当前任务对应的一个阶段文件；跨阶段任务按顺序逐个读取，不一次加载全部阶段文件。历史题材、地方文化题材或以真实历史为宣传依据的项目，还要按需读取 [references/historical-production.md](references/historical-production.md)；非历史项目不加载该文件。

- 整剧构思、分集大纲、金钩、情绪线：读取 [references/story.md](references/story.md)。
- 写剧本、改剧本、对白、正常短剧格式：读取 [references/script.md](references/script.md)。
- 导演拆解、镜头设计、表演调度、光影声音：读取 [references/director.md](references/director.md)。
- 资产提取、角色定妆、场景/道具/特效生图提示词：读取 [references/assets.md](references/assets.md)。
- 分集视频提示词、参考图片映射、对白与口型：读取 [references/video.md](references/video.md)。

## Skill 异常报备与受控进化

- 返工产物时先定位问题所属阶段，默认只修改剧本或当前阶段产物。生产模式中Skill入口、阶段参考、质量门禁、模板和检查脚本始终冻结。
- 工作中怀疑 Skill 不合理时，不得自行改写、绕过或追加规则，也不得把当前项目的一句写法、一次失败或个人偏好升级为全局要求。先判断问题来自产物执行不当、项目特例，还是可在不同项目复现的 Skill 缺陷。
- 只有同类问题经过多次返工仍未通过，出现可复现的误判、漏判、规则冲突与质量降级，或用户明确要求全面审计Skill时，才提交Skill修改申请。申请必须一次写清具体文件与规则、复现证据、质量影响、完整修改范围、阶段影响和验证方法，不拆成逐条试改。
- 提交申请后暂停 Skill 层修改；在不牺牲质量的前提下可以继续处理不受影响的产物。若现行规则会强迫产物继续降质，则停止受影响部分并报告阻塞，不以暗改 Skill 或故意绕过检查解决。
- 只有用户明确批准所提 Skill 修改后，才进入受控进化：先备份当前 Skill，执行获批的最小修改，用正例、反例和官方结构校验验证规则，再局部重跑受影响产物。不得借一次批准扩改未报备的文件或规则。
- 修改完成后向用户报告实际改动、验证结果、影响范围和备份位置。未获得明确批准时，任何情况下都不得改动全局 Skill。

## 生产顺序

用户未指定起点时：

项目事实基准 → 故事架构与分集大纲 → 正常短剧剧本 → 导演分解 → 全剧资产提示词 → 分集视频提示词。

- 现有项目先识别并复用已经承担事实基准职责的文件，不因文件名不同重复创建。新建完整流程项目或确实没有等价基准时，才默认建立`项目创作基准.md`，只保存用户冻结要求、题材/集数/时长、人物与世界观硬约束、禁止项、制作方向；历史项目同时保存史实、推定、传说、虚构和年代视觉边界。它是下游共同事实入口，不复制各阶段正文。
- `pipeline-state.md`只用于跨阶段、需要中断续跑的完整生产任务，记录当前阶段、正式产物路径、审核状态、用户是否确认和下一步；单阶段创作、检查、返工、旧稿审查和一次性交付不强制创建。任何情况下都不保存长篇审查、哈希链或剧情副本。
- 用户明确要求某一阶段时只完成该阶段。用户要求完整流程时可以按顺序连续执行文本阶段；若用户采用逐阶段确认，则在其确认冻结前不进入下一阶段。

- 用户要求单集，只处理该集及其必要上游，不擅自跑完整流水线。
- 用户要求某一阶段，只交付该阶段，不提前生成下游。
- 上一阶段没有通过质量检查时，留在原阶段局部修复。
- 阶段交接传正式产物，不传推理记录、审查日志、哈希证据或重复剧情副本。
- 检查既有产物时先识别其采用的合同版本。新产物执行当前合同；旧产物默认按原合同审查内容，不因新字段或新槽位格式强制全量迁移。只有用户要求升级格式，或旧格式无法完成当前下游交接时，才执行明确、局部、可说明的迁移。

## 不可删除的硬性红线

1. 正式剧本使用正常短剧剧本格式，不能写成逐秒分镜、`Bn/Vn` 节拍表或字段堆砌。
2. 每集必须有具体事件、人物目标、有效阻力、主动行动、状态变化、局部兑现或代价，以及因果产生的结尾推动；金钩和情绪变化不能只剩标签。
3. 不设置正文总字数、场景数、对白数、节拍数或角色数配额。质量由因果、节奏、变化、人物和可制作性判断。
4. 导演阶段负责把剧本转化为镜头、表演、光线、声音和可生成片段；不得把导演工作反塞进剧本。
5. 用户冻结要求、已确认的世界观与历史边界、通过的正式剧本共同约束项目事实；正式剧本是当前场次人物行动、对白、关系、证据、资源和剧情因果的直接依据，历史形制与视觉细节则依据已确认研究和全剧美术体系。导演可以在不改变剧情事实的前提下补足服务表演、构图、光声和空间可信度的非剧情性置景、生活器具、环境细节与不具名背景人口，并可把不影响因果的剧本泛称等价转译为符合时代和地域的视觉形式。补充应最少但有效，不得新增剧情人物、关键物件、证据、能力、组织、地点或因果。连续性按同镜、同场、复用场景和跨场跨集分级管理；普通物件不得仅因一次特写或同场复用就强制建立独立资产。
6. 全剧资产提示词统一交付为一个 Markdown 文件，不按资产拆分文件。用户未指定布局时使用Skill默认资产板合同；用户明确批准外部模板时以该模板为项目冻结合同，不再被默认比例覆盖。默认合同下人物和道具使用纯白背景，场景不强制白底。提示词不输出来源集数、节拍编号、哈希、剧本转储或工作命令。
7. 资产阶段只生成生图提示词；视频阶段只生成视频提示词。不得调用图片或视频生成工具。
8. 视频提示词必须逐字保留所属剧本对白，明确说话人、声音来源和口型边界；时长不够就拆段，不能删对白。
9. 视频阶段不生成、不读取、不审核参考图片。人物五官、体型、发型、基础服装、场景结构和道具造型由资产提示词及工作人员实际传入视频模型的参考图锁定；视频提示词不得重新设计这些静态事实。每段必须保留独立`【风格】`模块，用于锁定全剧影像媒介、色彩体系、场景冷暖、真实光源、材质、空气和摄影质感；正文只负责动作、表情、表演、摄影、动态光影、声音和结束状态。

## 强制轻量发布门

任何正文、导演包或提示词交给用户前，都必须执行 [references/quality-gate.md](references/quality-gate.md)。不能因为赶时间、批量生产、篇幅较长或“基本能用”而跳过。

同一版本固定流程只有四步：生成一次 → 静默检查一次 → 失败项局部修复一次 → 再检查一次。第二次仍失败时停止交付成品，只报告具体阻塞项；不得把未通过稿件包装成完成结果。用户提供新反馈后形成新版本，可以开始新的受控检查周期；同类缺陷连续三个用户版本仍重复出现时停止盲目返工，报备上游或Skill异常。

门禁不得启动子代理，不建立大型 JSON、证据卡、冻结包、哈希链或重复剧情副本，不把检查过程输出给用户。单个产物最多检查两次，禁止循环审稿。

发现问题只返修问题所在的集、场、镜头或资产；除非上游根基已改变，不全剧重跑。

## 批量效率

- 长剧先完成整剧架构和分集发动机检查，再批量写剧本，避免写到中途才发现容量不足。
- 同一阶段可以连续生产多集，检查汇总执行，但每集仍需读到并处理自己的目标、冲突、变化和交接。
- 不为“形式完整”增加无生产价值的副文件。默认交付 Markdown 正文。
- 机械检查只拦格式和闭合错误；创作质量按各阶段文件中的语义检查处理，不能用字段齐全代替好内容。
- 批量门禁只保留“单元编号＋失败代码”短表，最多一行一个单元；通过后不落盘、不向用户展示。
